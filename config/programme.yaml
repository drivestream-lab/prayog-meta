apiVersion: launchpad/v1
kind: Programme
programme: prayog
programme_slug: prayog
org: drivestream-lab
meta_repo: prayog-meta
forge:
  provider: github

# Programme repos (logical):
#   prayog-meta, prayog-skills, launchpad  — platform / control plane
#   gateflow (API), gateflow-ops (BFF)      — Gateflow delivery runtime (planned)
# Workspace (local): ~/Workspace/handson/drivestream-lab
